server:
  port: 9095

spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}

codeperf:
  storage:
    mode: ${CODEPERF_STORAGE_MODE:memory}

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    banner: false
