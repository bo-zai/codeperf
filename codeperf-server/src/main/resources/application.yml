server:
  port: 9095

spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
  datasource:
    hikari:
      maximum-pool-size: ${CODEPERF_DB_MAX_POOL_SIZE:3}
      minimum-idle: ${CODEPERF_DB_MIN_IDLE:0}
      connection-timeout: ${CODEPERF_DB_CONNECTION_TIMEOUT_MS:5000}

codeperf:
  storage:
    mode: ${CODEPERF_STORAGE_MODE:memory}
  agent:
    install:
      enabled: ${CODEPERF_AGENT_INSTALL_ENABLED:true}
      server-url: ${CODEPERF_AGENT_SERVER_URL:http://127.0.0.1:9095}
      agent-url: ${CODEPERF_AGENT_URL:http://127.0.0.1:9095/api/agent/artifact}
      agent-sha256: ${CODEPERF_AGENT_SHA256:}
      local-artifact-path: ${CODEPERF_AGENT_LOCAL_ARTIFACT_PATH:codeperf-agent/target/codeperf-agent.jar}
      target-packages: ${CODEPERF_AGENT_TARGET_PACKAGES:com.cmb.codeperf.demo}
      entry-method: ${CODEPERF_AGENT_ENTRY_METHOD:POST}
      entry-path: ${CODEPERF_AGENT_ENTRY_PATH:/}
      slow-sql-ms: ${CODEPERF_AGENT_SLOW_SQL_MS:500}
      sample-ms: ${CODEPERF_AGENT_SAMPLE_MS:10}
      mode: ${CODEPERF_AGENT_MODE:session}

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    banner: false

